using UnityEngine;

namespace HS.Framework.Gameplay
{
    /// <summary>
    /// 게임 흐름 신호를 받아 스스로 자기 상태를 갖는 컴포넌트의 표식이다.
    /// </summary>
    /// <remarks>
    /// <para>
    /// <b>멤버가 없다.</b> 어떤 신호를 듣고 어떤 상태를 갖는지는 게임마다 다르고, 그 상태의 모양을
    /// 미리 정해 둘 만큼 공통된 것이 없다. 이 표식이 강제하는 것은 딱 하나 — <b>신호는 이벤트로 받는다</b>는
    /// 것뿐이며, 그 규약조차 타입으로 강제하지 않는다. 프레임워크가 이벤트 버스를 이미 갖고 있고
    /// (<c>MessagePipe</c>), 상속으로 훅을 만들면 그 훅을 안 쓰는 구현이 생기는 순간 표식과 실제가 어긋난다.
    /// </para>
    /// <para>
    /// <b>다른 시스템을 직접 참조하지 않는다.</b> 이 표식을 상속하는 컴포넌트는 신호를 준 쪽을 몰라도 된다 —
    /// 신호를 준 쪽이 이벤트를 발행하고, 이 컴포넌트가 구독해 자기 상태를 스스로 바꾼다. 직접 참조로
    /// 묶으면 신호를 주는 쪽이 바뀔 때마다 받는 쪽도 함께 고쳐야 한다.
    /// </para>
    /// </remarks>
    public abstract class GameState : MonoBehaviour
    {
    }
}
